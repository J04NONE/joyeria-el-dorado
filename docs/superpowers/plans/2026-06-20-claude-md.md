# CLAUDE.md del proyecto — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Crear un único archivo `CLAUDE.md` en la raíz real del repositorio que
resuma el proyecto Joyería El Dorado, para que Claude Code lo cargue
automáticamente al iniciar sesión y no tenga que re-explorar el repo cada vez.

**Architecture:** Archivo markdown estático, sin generación automática ni
sincronización. Vive en `<raíz del repo>/CLAUDE.md` (la carpeta que contiene
`backend/`, `docs/` y `.git/` — en disco esa carpeta se llama `frontend`, pero
**no** confundir con la subcarpeta anidada `frontend/` que contiene el cliente
web). Contenido fijado por el spec: qué es el proyecto, estructura real,
comandos esenciales, base de datos, convenciones, gotchas conocidos.

**Tech Stack:** Markdown plano. Sin dependencias, sin build, sin tests
automatizados (es documentación, no código ejecutable).

---

## Spec de referencia

`docs/superpowers/specs/2026-06-20-claude-md-design.md`

## File Structure

- Create: `CLAUDE.md` (en la raíz del repo, junto a `README.md` y `.gitignore`)

Un solo archivo. No se requiere ninguna otra modificación de código.

---

### Task 1: Confirmar la ruta de destino exacta

**Files:**
- (ninguno se crea todavía; este task solo verifica la ruta)

- [ ] **Step 1: Verificar que estás en la raíz real del repo**

Run: `git rev-parse --show-toplevel`

Expected output (ruta absoluta de la raíz del repo git):
```
C:/Users/muril/Desktop/Sena/Aplicacion/JOYERIA/frontend
```

- [ ] **Step 2: Confirmar que `CLAUDE.md` no existe todavía ahí**

Run: `ls CLAUDE.md 2>&1 || echo "no existe, OK para crear"`

Expected: `no existe, OK para crear` (si ya existiera un `CLAUDE.md`, detenerse y
preguntar al usuario antes de sobrescribir).

- [ ] **Step 3: Confirmar que NO se va a escribir dentro de la subcarpeta anidada `frontend/`**

Run: `ls frontend/CLAUDE.md 2>&1 || echo "correcto: no se toca frontend/ anidado"`

Expected: `correcto: no se toca frontend/ anidado`

---

### Task 2: Escribir el archivo CLAUDE.md

**Files:**
- Create: `CLAUDE.md` (raíz del repo)

- [ ] **Step 1: Crear el archivo con el contenido completo**

Contenido exacto a escribir en `CLAUDE.md`:

```markdown
# Joyería El Dorado — Contexto del Proyecto

Sistema de gestión full-stack para una joyería. Backend Django 5.2 + Django
REST Framework 3.16 + JWT (SimpleJWT). Frontend HTML5 + CSS3 + JavaScript
(ES6+) vanilla, sin framework.

## Estructura real (tal como está hoy en disco)

- `backend/` — proyecto Django. Apps: `autenticacion`, `clientes`,
  `inventario`, `ventas`, `proveedores`. Config principal en
  `backend/joyeria_backend/`.
- `css/` y `js/` — en la **raíz del repo** (no dentro de `frontend/`).
- `frontend/` — subcarpeta anidada con `assets/` y `pages/` (los `.html`).
- `docs/` — documentación adicional (`findings.md`, `task_plan.md`,
  `progress.md`, `roles.md`). Nota: `task_plan.md` describe una reorganización
  de carpetas que quedó parcialmente aplicada — no asumir que `css/`/`js/` ya
  están movidos dentro de `frontend/`.

## Comandos esenciales

```bash
cd backend
env_joyeria\Scripts\activate        # Windows
pip install -r requirements.txt
python manage.py migrate
python manage.py setup_groups       # crea grupos Administradores/Empleados/Clientes
python manage.py runserver
python manage.py test --keepdb      # ver gotcha de Neon abajo
```

Frontend (servidor estático simple):
```bash
cd frontend  # o la raíz, según desde dónde se sirvan los HTML
python -m http.server 8000
```

## Base de datos

- **Por defecto:** SQLite (`backend/db.sqlite3`), sin configuración extra.
- **Neon (PostgreSQL serverless):** se activa definiendo `DATABASE_URL` en
  `backend/.env`. Si esa variable existe, tiene prioridad sobre SQLite (ver
  `backend/joyeria_backend/settings.py`). Usa `sslmode=require`,
  `CONN_HEALTH_CHECKS=True` y `DISABLE_SERVER_SIDE_CURSORS=True` (requerido
  por el connection pooler de Neon).
- **Gotcha:** el pooler de Neon puede mantener una sesión abierta y bloquear
  el `DROP DATABASE` al final de `python manage.py test`. Si aparece
  `database "test_..." is being accessed by other users`, correr con
  `python manage.py test --keepdb`.

## Convenciones

- **Roles del sistema:** administrador, vendedor, cliente, invitado (ver
  `docs/roles.md` para el detalle de accesos por rol).
- **Autenticación:** JWT vía `/api/auth/` (login, register, profile,
  token/refresh).
- **Documentación interactiva de la API:** Swagger UI en
  `/api/schema/swagger-ui/`, ReDoc en `/api/schema/redoc/` (drf-spectacular).

## Gotchas conocidos

- **Encoding en consola de Windows:** algunos comandos de gestión (ej.
  `setup_groups`) escriben emojis a stdout. La consola de Windows usa cp1252
  por defecto y puede fallar con `UnicodeEncodeError`. Si pasa, correr con
  `PYTHONIOENCODING=utf-8` antes del comando.

## Mantenimiento de este archivo

Se actualiza a mano cuando se descubre un gotcha nuevo o cambia la estructura
real del proyecto. No se genera ni se sincroniza automáticamente.
```

- [ ] **Step 2: Verificar que el archivo se escribió en la ruta correcta**

Run: `git rev-parse --show-toplevel && ls -la CLAUDE.md`

Expected: la ruta del paso anterior coincide con el directorio actual, y
`CLAUDE.md` aparece listado con tamaño > 0 bytes.

---

### Task 3: Verificar que el contenido es preciso

**Files:**
- Read-only: `backend/joyeria_backend/settings.py`, `README.md`,
  `docs/roles.md` (si existe)

- [ ] **Step 1: Comparar la sección de roles del CLAUDE.md contra `docs/roles.md`**

Run: `ls docs/roles.md`

Si el archivo no existe en esa ruta exacta, buscarlo:
Run: `find . -iname "roles.md" -not -path "*/env_joyeria/*"`

Ajustar la referencia en `CLAUDE.md` (sección "Convenciones") a la ruta real
encontrada. Si no existe ningún `roles.md`, quitar la referencia y dejar solo
la lista de roles.

- [ ] **Step 2: Confirmar que `DATABASE_URL` y los flags de Neon mencionados existen en settings.py**

Run: `grep -n "DATABASE_URL\|CONN_HEALTH_CHECKS\|DISABLE_SERVER_SIDE_CURSORS\|sslmode" backend/joyeria_backend/settings.py`

Expected: las 4 cadenas aparecen (ya fueron agregadas en una sesión anterior
al implementar la conexión a Neon). Si alguna falta, corregir la sección
"Base de datos" del `CLAUDE.md` para que coincida con el código real.

---

### Task 4: Commit

**Files:**
- `CLAUDE.md` (nuevo)

- [ ] **Step 1: Agregar y commitear**

```bash
git add CLAUDE.md
git commit -m "docs: agregar CLAUDE.md con contexto del proyecto

Resume stack, estructura real, comandos, base de datos y gotchas
conocidos para que Claude Code no tenga que re-explorar el repo en
cada sesión nueva.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

- [ ] **Step 2: Confirmar el commit**

Run: `git log -1 --stat`

Expected: el commit más reciente muestra `CLAUDE.md` como archivo agregado.

---

## Notas para quien ejecute el plan

- No hay tests automatizados porque el artefacto es documentación, no código.
  La "verificación" de este plan es: (a) el archivo queda en la ruta correcta,
  (b) su contenido es factualmente exacto contra el código actual, (c) queda
  commiteado.
- Task 3 puede revelar que algún dato cambió desde que se escribió este plan
  (ej. settings.py fue modificado de nuevo). En ese caso, el contenido de
  `CLAUDE.md` debe ajustarse a la realidad del código, no al texto literal de
  este plan.
